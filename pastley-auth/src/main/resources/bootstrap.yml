spring:
  application:
    name: pastley-auth
  cloud:
    config:
      enabled: true
      uri: http://localhost:8084
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
